// Shared UI timing constants (milliseconds).
export const HOVER_OPEN_DELAY_MS = 300; // delay before a card expands on hover
export const PROVIDER_FETCH_DELAY_MS = 400; // debounce before fetching providers on hover
export const SEARCH_DEBOUNCE_MS = 350; // debounce before firing a search request
export const HERO_ROTATE_INTERVAL_MS = 6000; // hero gallery auto-rotate interval
